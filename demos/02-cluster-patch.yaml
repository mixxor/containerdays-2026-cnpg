# Demo 2b — wire the plugin into the Cluster. Sidecar appears on the next rollout.
spec:
  plugins:
  - name: barman-cloud.cloudnative-pg.io
    isWALArchiver: true                          # this plugin owns WAL archive
    parameters:
      barmanObjectName: hafen-store
